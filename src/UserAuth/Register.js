import React, { Component } from 'react';

class Register extends Component {
	render() {
		return (
			<div>
				{/* <input onChange={(e) => {this.setState({email: e.target.value})}}
				type="email"
				placeholder="email" />
				<button onClick={() => this.setState({isRegistering: false, isLoginedin: true})} className="No-focus"> Register </button> */}
			</div>
		);
	}
}

export default Register;